# Studio Exports

Drop exported `.arcana.json` files here before running `npm run studio:apply`.

Workflow:
1. Export JSON from Arcana Studio.
2. Put the downloaded file in this folder.
3. Run `npm run studio:apply`.
4. Applied files are moved into `studio_exports/archived`.
